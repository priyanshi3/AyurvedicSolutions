<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ayurvedicsolutions">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AyurvedicSolutions.NoActionBar">
        <activity
            android:name=".activity.SubCategory"
            android:exported="false" />
        <activity
            android:name=".activity.DataDisplay"
            android:exported="false" />
        <activity
            android:name=".activity.DataDisplayLifeMedicineActivity"
            android:exported="false" />
        <activity
            android:name=".activity.DataDisplayRogBeautyActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>